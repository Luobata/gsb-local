# One independent command per pane.
# Each line activates one role. Remove a line and rebuild to remove its pane.
# Built-ins: claude, codex, kimi
# Compatible wrapper: claude:<binary>, codex:<binary>, or kimi:<binary>
# Fully custom command: shell:<command>

hub=codex
core-bug=claude-0812
ops-gov=claude-relay
plan-backup=codex
ui=kimi
